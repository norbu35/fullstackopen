const { Op } = require("sequelize");
const resetPassWordMap = new Map();

// await User.findOne({
//     where: {
//         userName,
//         deleteTime: {
//             [Op.is]: null
//         },
//     },
//     attributes: ['uId', 'userName', 'passWord', 'phone', 'isUserState']
// })

// await UserInfo.findOne({
//     where: {
//         uId,
//         deleteTime: {
//             [Op.is]: null
//         },

//     },
//     attributes: { exclude: ['id', 'deleteTime'] }
// })

// await User.update({ passWord }, { where: { userName } });